<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="CheckboxTheme" parent="AppTheme">
        <item name="colorAccent">@color/dark_red</item>
    </style>

    <style name="CheckBox" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/checkBox_red</item>
        <item name="colorControlActivated">@color/checkBox_red</item>
        <item name="android:textSize">16sp</item>
        <item name="android:paddingStart">32dp</item>
    </style>

    <style name="MyWidget.MaterialComponents.Chip.Choice" parent="Widget.MaterialComponents.Chip.Choice">
        <!-- Chip background color selector -->
        <item name="chipBackgroundColor">@color/white</item>
        <!-- Border color -->
        <item name="chipStrokeColor">@color/transparent</item>

        <!-- Chip text color selector -->
        <item name="android:textColor">@color/chip_colors</item>
    </style>

    <style name="ColorSwitchStyle">
        <item name="colorControlActivated">@color/red</item>
        <item name="colorControlNormal">@color/red</item>
    </style>
    <style name="MaterialButtonStyle" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="backgroundTint">@color/color_states_materialbutton</item>
    </style>
    <style name="RatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="colorControlNormal">@color/red</item>
        <item name="colorControlActivated">@color/gold</item>
    </style>

    <style name="CustomSwitch" parent="Theme.MaterialComponents.Light">
        <item name="colorControlActivated">#45D09E</item>
        <item name="colorSwitchThumbNormal">#ECECEC</item>
        <item name="android:colorForeground">#464646</item>
    </style>

    <style name="ChipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/chip_background</item>
        <item name="android:textColor">@color/chip_colors</item>
    </style>
</resources>
